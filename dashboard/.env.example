# Sentinel Dashboard — Environment Variables
# Copy to .env.local for local development.
# Backend API URL (Next.js rewrites /api/* to this when using Vercel config)
NEXT_PUBLIC_API_URL=http://localhost:8000
# API key for authenticated backend endpoints (must match backend API_KEY)
NEXT_PUBLIC_API_KEY=
